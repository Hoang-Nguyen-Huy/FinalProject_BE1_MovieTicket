package util;

import java.util.HashMap;
import java.util.Map;

import user_roles.User;

public class AccountManager {
    Map<String, User> users = new HashMap<>();
    public AccountManager() {
    }
    
    void addNewUser(User newUser) {
        users.put(newUser,1``323`` )
    }
}
